# Hedera Vanilla JS Wallet ConnectIntegration
A decentralized application template in pure JavaScript, HTML, and CSS. It uses Vite as its build tool.

## 
To run:

```
npm install
```

```
npm run dev
```